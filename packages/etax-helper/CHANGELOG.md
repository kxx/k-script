# 更新日志

## 1.2.5

- 统一为浅灰工作区、白色内容区和靛蓝主色，保留纸飞机及现有功能。
- 收紧顶部、表单和空状态布局；平台选择使用中性分段样式，账户配置突出主操作。
- 统一控件高度、字体、边框和圆角，调整请求与设置页，底栏地区使用中文显示。
- 限制面板溢出，表单按需滚动；补充窄屏布局。

## 1.2.4

- 隔离配置读取和请求采集初始化失败；支持分别重试。
- 设置中增加默认折叠的本机运行诊断，复制内容排除配置、请求及原始异常。
- XHR 安装回退、重复安装保护和退出清理；页脚显示实际采集状态。
- 增加生成产物浏览器回归和 CI 检查；保留纸飞机、当前布局、业务端点与登录规则。
- 工具链升级留待本版现场验收后单独进行。

## 1.2.3

- 根据现场 `x0 is not a function` 堆栈定位配置读取：旧构建通过 window.GM_getValue 取值，但油猴可能仅提供脚本作用域绑定。
- 统一改为直接调用作用域中的 GM API，包括存储、请求、打开标签和剪贴板；unsafeWindow 同样从作用域读取。
- 显式声明 grant，并在构建时禁止重新引入 window 属性访问方式。
- 配置读取/旧配置迁移异常不再阻止入口显示；设置页提供权限错误提示。
- Chromium 对照测试复现旧版同一 x0 错误；新版在 GM API 不挂载到 window 的环境中通过入口及交互验证。

## 1.2.2

- 修复内置 lodash-es 在油猴对象隔离与严格 CSP 下通过 Function 获取全局对象，导致入口初始化中断的问题；改为 globalThis，不修改主系统 CSP。
- 入口挂载到文档根节点，并在宿主被移除时恢复，避免主系统重绘 body 后丢失纸飞机。
- 补充固定 updateURL/downloadURL，构建时检查版本、更新地址及动态执行代码。
- 在 Chromium 的严格 CSP + 模拟隔离对象环境中验证旧版报错、新版正常显示入口。

## 1.2.1

- 恢复原有 Promotion 纸飞机悬浮入口，重做简洁抽屉布局及账户/请求导航。
- 设置改为右上角入口；手动登录、请求详情在抽屉内展示，隐藏未接通的授权操作。
- 支持拖动及键盘调宽、宽度记忆、窄屏适配与键盘焦点管理。
- 请求表格展示路径、方法、状态、耗时；详情支持安全 JSON 高亮、切换参数/响应、解密及脱敏复制。
- Vue、Element Plus 和图标内置打包，移除 CDN 和 window.Vue 桥接；样式及弹层保留于 Shadow DOM，面板不修改主系统 body。
- 修复表格响应式代理导致采集 Token 无法匹配的问题。
- 在真实 Chromium 的 Vue 2 + Element UI 模拟主系统中完成兼容性和交互回归。

## 1.2.0

- 账户、请求、设置改为明确标签，接通 API Key 配置和账户搜索。
- XHR/Fetch 监听独立于面板，避免切换模式重复采集；限制记录数量和正文大小。
- 兼容文本、JSON、二进制响应以及错误、超时、取消；保留页面原始请求返回行为。
- 手动登录增加参数和 Cookie 写入检查，落实新标签页设置。
- 统一后端超时与错误处理；解密绑定采集时的 Token，防止过期结果覆盖。
- 配置迁移至脚本管理器；Token 隐藏显示，复制常见敏感字段时脱敏。
- 使用 Shadow DOM 隔离面板样式及弹层；清理未使用代码和重复监听实现。
- 保留现有地区入口、跨平台 Cookie 作用域和后端接口契约；未完成的自动授权明确禁用。
- 统一版本来源，增加回归测试、构建和产物一致性检查。

## 回退

1. 保留已安装的脚本，不要先卸载；先核对是否需要保留当前设置。
2. 使用基线提交中的 `packages/etax-helper/dist/etax-helper.user.js` 覆盖安装，并核对版本 1.2.3。
3. 若需停留在旧版，暂时关闭该脚本自动更新，避免固定 main 更新地址再次拉取新版；问题解决后恢复更新。
